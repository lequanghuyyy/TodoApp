/**
 * REST controllers layer.
 *
 * <p>Each controller handles HTTP requests for a specific resource,
 * delegates business logic to the {@link com.example.todoapp.service} layer,
 * and returns appropriate HTTP responses.
 *
 * <p>Naming convention: {@code <Resource>Controller}
 */
package com.example.todoapp.controller;
