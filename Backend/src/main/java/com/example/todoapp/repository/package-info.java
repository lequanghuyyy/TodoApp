/**
 * Repository layer.
 *
 * <p>Spring Data JPA repositories for database access.
 * Each repository extends {@link org.springframework.data.jpa.repository.JpaRepository}
 * and may add custom query methods.
 *
 * <p>Naming convention: {@code <Entity>Repository}
 */
package com.example.todoapp.repository;
