/**
 * Shared constants and enumerations.
 *
 * <p>Centralises magic strings, numeric limits, and status enums
 * to avoid scattering literals across the codebase.
 *
 * <p>Typical contents:
 * <ul>
 *   <li>{@code TodoStatus} – task completion states</li>
 *   <li>{@code ApiConstants} – URL prefixes, header names, etc.</li>
 * </ul>
 */
package com.example.todoapp.constant;
