/**
 * MapStruct mapper interfaces.
 *
 * <p>Mappers convert between {@link com.example.todoapp.entity} objects
 * and {@link com.example.todoapp.dto} objects at compile time.
 *
 * <p>All mappers use Spring component model so they can be injected
 * via {@code @Autowired} / constructor injection.
 *
 * <p>Naming convention: {@code <Entity>Mapper}
 */
package com.example.todoapp.mapper;
