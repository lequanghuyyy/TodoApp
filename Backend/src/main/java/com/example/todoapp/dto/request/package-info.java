/**
 * Inbound request DTOs.
 *
 * <p>Objects representing payloads received from HTTP clients.
 * All fields should be annotated with Jakarta Validation constraints
 * to enforce input correctness before reaching the service layer.
 */
package com.example.todoapp.dto.request;
